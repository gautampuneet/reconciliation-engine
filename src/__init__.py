"""
Reconciliation Engine implementation (production-grade).

This package is intentionally namespaced under `src` to avoid accidental imports
from the legacy top-level `engine/`, `models/`, and `strategies/` packages.
"""

